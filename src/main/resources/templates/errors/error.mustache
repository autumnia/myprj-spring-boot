{{>layout/header}}

<div class="container">
    <div class="col-md-12">
        <p> 공통 에러 페이지 </p>
        {{ #exception.getStackTrace() }}
            {{ toString() }}
        {{ /exception }}
    </div>
</div>

{{>layout/scripts}}
{{>layout/footer}}
